@{
    ViewData["Title"] = "Home Page";
    ViewData["ServerUrl"] = "localhost:5000/login";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h2>@ViewBag.id</h2>
    <h2>@ViewBag.token</h2>
    <a href="@ViewData["ServerUrl"]/login">
        <button>Login to Spotify Right Now or else</button>
    </a>
</div>
