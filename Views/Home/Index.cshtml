@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h2>@ViewBag.id</h2>
    <h2>@ViewBag.token</h2>
    <a href="">
        <button>Login to Spotify Right Now or else</button>
    </a>
</div>
